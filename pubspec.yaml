name: project_bengkalis
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.1
  equatable: ^2.0.7
  get_it: ^7.2.0
  dio: ^5.4.3+1
  chucker_flutter: ^1.8.5
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  hive_generator: ^1.1.0
  build_runner: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
